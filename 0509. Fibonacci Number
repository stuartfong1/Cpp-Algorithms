// Dynamic Programming
class Solution {
public:
    int fib(int N) {
        int dp[31];
        dp[0] = 0;
        dp[1] = 1;
        dp[2] = 1;
        dp[3] = 2;
        for(int i = 4; i <= N; ++i)
        {
            dp[i] = (dp[i-1]+dp[i-2]);
        }
        return(dp[N]);
    }
};
